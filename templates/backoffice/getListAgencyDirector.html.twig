{% include 'includes/header.html.twig' %}
<div class="principale">
<h1>Fiche des directeurs d'agences : </h1>
Nombre d'agents : {{ ListAgencyDirector | length }}
<ul>
    {% for agent in ListAgencyDirector %}
        <li>{{ agent.user.email }} <a href="{{ path('modifyAgencyDirector',{'idAgencyDirector' : agent.id })}}">Modifier</a><a href="{{ path('removeAgent',{'idAgent' : agent.id })}}">Supprimer</a></li>
    {% endfor %}
</ul>

</div>
</body>
</html>
