<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>{% block title %}Welcome!{% endblock %}</title>
    {% block stylesheets %}<link href="{{ asset('css/main.css')}}" rel="stylesheet">
        <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css" integrity="sha384-ggOyR0iXCbMQv3Xipma34MD+dH/1fQ784/j6cY/iJTQUOhcWr7x9JvoRxT2MZw1T" crossorigin="anonymous">{% endblock %}
</head>
<body>

{% block body %}
{% endblock %}
{% block javascripts %}{% endblock %}
<ul class="navbartest">
    {% if is_granted('ROLE_ADMIN') %}
    <li><a href="{{ path('backoffice') }}">Backoffice</a></li>
    {% endif %}
    {% if is_granted('ROLE_AGENT') %}
        <li><a href="{{ path('possession_manage', {'agentId': agent.id}) }}">Manage Possessions</a></li>
    {% endif %}
    <li><a href="{{ path('possession_app_possession_createpossessionbyagent') }}">Create possession by agent</a></li>
    <li><a href="{{ path('possession_list') }}">Search possession</a> </li>
    {% if app.user %}
    <li><a href="{{ path('logout') }}">logout</a></li>
    {% else %}
    <li><a href="{{ path('app_login') }}">login</a></li>
    {% endif %}
</ul>
{% include 'account/infosAccount.html.twig' %}
<h1>ImmoConceptPlus<br>Une maison, une idée, un concept</h1>

<div class="principale">
    <h2>Les offres du moment</h2><br>
    {% if possessions %}
    {% for possession in possessions %}
        <div class="lastOffer">
        {% if possession.possessionImage.first %}
        <img class="imgSearchPossession" src="{{ asset(vich_uploader_asset(possession.possessionImage.first, 'imageFile', 'App\\Entity\\PossessionImage')) }}" alt="possessionImage"><br>
        {% endif %}
        {{ possession.title }}
        </div>
    {% endfor %}
    {% endif %}
</div>




</body>
</html>
